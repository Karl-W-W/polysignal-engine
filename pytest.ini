[pytest]
testpaths = tests
pythonpath = .
markers =
    network: reaches the network in a full run (proven 2026-09-10: the e2e module hung 20 min on an external socket in the deploy handler while passing alone); the deploy handler deselects it with -m 'not network'
